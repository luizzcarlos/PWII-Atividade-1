let numero1 :number =2
let fParImpar1 = function (num:number) :number {
    return num%2;
}  
    if (fParImpar1(numero) == 0) {
        console.log (`Esse número é par: ${numero}`)
    }
    else {
        console.log (`Esse número é ímpar: ${numero}`)
    }
